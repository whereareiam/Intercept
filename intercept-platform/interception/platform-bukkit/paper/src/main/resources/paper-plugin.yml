name: @projectName@
main: paper.platform.me.whereareiam.intercept.platform.interception.PaperIntercept
version: '@projectVersion@'
api-version: '1.20'
folia-supported: true
authors:
  - whereareiam

dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true
    packetevents:
      load: BEFORE
      required: false
      join-classpath: true