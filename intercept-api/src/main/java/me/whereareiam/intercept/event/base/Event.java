package me.whereareiam.intercept.event.base;

/**
 * Base interface for all events in the Intercept plugin.
 * This interface serves as a marker to identify event classes and provides
 * a common type for event handling mechanisms.
 * <p>
 * All custom events in the plugin should implement this interface to ensure
 * compatibility with the plugin's event handling system.
 */
public interface Event {
}